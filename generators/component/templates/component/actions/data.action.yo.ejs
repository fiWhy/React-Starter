import { createActionAsync } from "redux-act-async";

export const <%= actionName %>Action = createActionAsync("<%= actionName %>Action", () =>{
    return Promise.resolve("<%= actionName %>Action");
})