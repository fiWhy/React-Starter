import * as React from "react";
import { create } from "react-test-renderer";
import "jest";
import <%= upperCamel %>Presentation from "./<%= dashed %>.presentation";

describe("<%= upperCamel %> Component Presentation", () => {
    const component = create(<<%= upperCamel %>Presentation />)
    const tree = component.toJSON();
    it("renders", () => {
        expect(tree).toMatchSnapshot();
    })
})