import * as React from "react";
import { create } from "react-test-renderer";
import "jest";
import <%= component.upperCamel %>Presentation from "./<%= component.dashed %>.presentation";

describe("<%= component.upperCamel %> Component Presentation", () => {
    const component = create(<<%= component.upperCamel %>Presentation />)
    const tree = component.toJSON();
    it("renders", () => {
        expect(tree).toMatchSnapshot();
    })
})