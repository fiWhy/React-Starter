import { createActionAsync } from "redux-act-async";

export const <%= action.upperCamel %>Action = createActionAsync("<%= action.upperCamel %>Action", () =>{
    return Promise.resolve("<%= action.upperCamel %>Action");
})